# Se puede combinar estas dos opciones utilizando export o source.
# Puede que quizás `source .secrets.env && ...` no funcione, por lo que,
# quizás sea mejor exportar las variables de entorno antes de ejecutar el comando.

apiVersion: v1
kind: Secret
metadata:
  name: questdb-secret
  namespace: development
  labels:
    app: simple-crud
    component: questdb
    environment: development
type: Opaque
stringData:
  QUESTDB_PASSWORD: ${QUESTDB_PASSWORD}

# Opción 1 (sin envsubst)

# kubectl create secret generic questdb-secret \
# --from-literal QUESTDB_PASSWORD=${QUESTDB_PASSWORD} \
# --namespace development

# Opción 2 (con envsubst)

# envsubst < k8s/development/questdb-secrets.yaml | kubectl apply -f - --namespace development
