# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Variables (uncommented just to keep them in github, but it's important to keep them in .gitignore)
# .secrets.env
# .env
